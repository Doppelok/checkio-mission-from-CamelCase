def from_CamelCase(name):
    #replace this for solution
    return NewName

if __name__ == '__main__':
    #These "asserts" using only for self-checking and not necessary for auto-testing
    assert from_CamelCase("MyFunctionName") == "my_function_name"
    assert from_CamelCase("IPhone") == "i_phone"
    assert from_CamelCase("ThisFunctionIsEmpty") == "this_function_is_empty"
    assert from_CamelCase("Name") == "name"

